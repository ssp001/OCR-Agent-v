## Minio storage buckket

This is a endpoint for s3 storage this is for storing psf and bytes data types in the project 
# run the server using:
- url-endpoint : "uvicorn api.v1.router.router_endpoint:app --reload"
